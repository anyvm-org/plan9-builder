ALL_RELEASES='"11952"'
# 9front deletes the previous image when publishing a new one, so
# the watcher swaps the release instead of appending (see
# base-builder/watch.py membership_update_mode).
ALL_RELEASES_UPDATE=replace
